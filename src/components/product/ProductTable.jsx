import React from 'react';

const ProductTable = ({product}) => {
    // console.log(product)
    return (
        <div>
            {
                product.map(product => )
            }
        </div>
    );
};

export default ProductTable;